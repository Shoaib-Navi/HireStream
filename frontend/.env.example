# Backend URL (without /api/v1)
VITE_API_URL=http://localhost:8000

# Never put secret keys in VITE_ variables: they are bundled into the public JavaScript.
# The chatbot's GEMINI_API_KEY belongs in backend/.env.
